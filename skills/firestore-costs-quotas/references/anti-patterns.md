# Firestore Anti Patterns

- Storing media blobs in documents.
- Arrays with unbounded growth.
- One global collection listener for app-wide state.
- Rules that depend on expensive document reads unnecessarily.
