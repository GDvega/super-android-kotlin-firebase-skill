# Android Testing Strategy

Prefer unit tests for domain and ViewModel behavior, repository tests for local/remote contracts, Compose UI tests for critical flows, instrumented tests for platform APIs, and Firebase emulator tests for rules/functions.
