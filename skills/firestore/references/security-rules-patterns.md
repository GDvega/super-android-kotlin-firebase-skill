# Security Rules Patterns

Rules should validate auth, path ownership, role membership, required fields, field types, immutable fields, and allowed transitions. Add allow/deny emulator tests for each sensitive path.
