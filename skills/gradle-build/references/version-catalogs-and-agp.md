# Version Catalogs And AGP

Keep Gradle, AGP, Kotlin and Compose compiler compatibility explicit. Use Firebase BoM for Firebase libraries. Prefer convention plugins when repeated build logic exists across modules.
