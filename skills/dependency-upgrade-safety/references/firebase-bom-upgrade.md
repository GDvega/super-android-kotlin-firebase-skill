# Firebase BoM Upgrade

- Keep Firebase Android libraries aligned through the BoM.
- Check Crashlytics, Messaging and Auth behavior after upgrades.
- Validate rules/emulator tests when data access changes.
