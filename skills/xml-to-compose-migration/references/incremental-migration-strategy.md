# Incremental Migration Strategy

- Start with leaf components or one screen.
- Keep old XML until all references are removed.
- Preserve ViewModel state contracts.
- Validate navigation and lifecycle behavior after each step.
