# Navigation 3 Patterns

- Prefer one clear navigation owner near the app shell.
- Keep route definitions small and typed when the project supports typed routes.
- Treat navigation as a UI event emitted from screen state, not business logic.
- Document route arguments, defaults and nullable values.
