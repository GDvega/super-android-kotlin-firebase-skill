# Firebase Setup Android

Use Firebase BoM, compatible Gradle plugins, separate dev/staging/prod projects, documented google-services handling, Emulator Suite for local validation, and product-specific rules before production use.
