# Known Migration Issues

- Plugin aliases may fail if repositories are misconfigured.
- Convention plugins can hide old AGP APIs.
- CI may use a different JDK or Gradle cache behavior than local machines.
