# Kotlin Plugin Compatibility

- Treat Kotlin, Compose, KSP and serialization versions as a compatibility group.
- Check annotation processors and compiler plugins before bumping Kotlin.
- Prefer stable releases for production apps.
