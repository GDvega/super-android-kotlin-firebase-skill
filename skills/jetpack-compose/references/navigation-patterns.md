# Navigation Patterns

Keep navigation at feature boundaries. Avoid passing NavController into reusable components. Prefer typed route models when the project supports them. Validate deep links and back behavior.
