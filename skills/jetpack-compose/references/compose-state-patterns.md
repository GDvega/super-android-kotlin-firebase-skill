# Compose State Patterns

Use Route composables for ViewModel wiring and Screen composables for stateless rendering. Hoist state, expose immutable UI state, model one-off effects intentionally, and add previews for loading, empty, content, error and permission-denied states.
