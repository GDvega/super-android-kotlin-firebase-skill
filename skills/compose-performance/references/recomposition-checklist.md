# Recomposition Checklist

Check unstable parameters, missing Lazy list keys, expensive state reads, unnecessary derived state, side-effect keys, image sizing and release-mode measurement before changing architecture.
